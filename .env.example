# Copy this file to .env and fill in your MySQL password
PORT=3001
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=
CORS_ORIGIN=http://127.0.0.1:5500,http://localhost:5500
YTDLP_PATH=
# Optional: ffmpeg binary path — required for YouTube HLS merging and MP3 extraction
# (lightweight option: pip install imageio-ffmpeg, then point this at its bundled exe)
FFMPEG_PATH=
